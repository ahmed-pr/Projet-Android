<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.vaccinationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vaccination">

        <activity android:name=".users.DashboardActivity"/>
        <activity android:name=".users.DownloadCertificateActivity"/>
        <activity android:name=".users.LoginActivity"/>
        <activity android:name=".users.RegisterActivity"/>
        <activity android:name=".users.RequestActivity"/>
        <activity android:name=".users.RequestFollowupActivity"/>
        <activity android:name=".users.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".admin.AcceuilAdmin" />
        <activity android:name=".admin.ValiderVaccination" />
        <activity android:name=".admin.ListeVaccin" />
        <activity android:name=".admin.ConfirmerValidation" />
        <activity android:name=".admin.Demande" />
        <activity android:name=".admin.ListeDemande"/>
    </application>

    <meta-data
        android:name="preloaded_fonts"
        android:resource="@array/preloaded_fonts" />

</manifest>